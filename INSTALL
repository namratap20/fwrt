To use this FWRT perform following

a)  Mysql user creation and grant permissions 
i.  	create db wrt_db;
ii. 	GRANT ALL ON wrt_db.* to 'wrt_user'@'localhost' identified by 'somepassword';

b) Fill in these mysql details alonge with admin's password and email.

c) All the mysql connection details would be stored in config.php.

d) Then login with user name as 'admin' and provided password.
