To use this FWRT perform following

a)  Mysql user creation and grant permissions 
i.  	create database wrt_db;
ii. 	GRANT ALL ON wrt_db.* to 'wrt_user'@'localhost' identified by 'somepassword';

b) untar 'fwrt' source at location accessible for the web server.

c) cd fwrt; touch config.php; make sure config.php is writable for the web server.

d) Fill in these mysql details alonge with admin's password and email.

e) All the mysql connection details would be stored in config.php.

f) Then login with user name as 'admin' and provided password.
